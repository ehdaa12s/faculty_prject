import 'package:flutter/material.dart';

class FS4 extends StatelessWidget {
  const FS4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
