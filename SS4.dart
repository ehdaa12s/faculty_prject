import 'package:flutter/material.dart';

class SS4 extends StatelessWidget {
  const SS4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
