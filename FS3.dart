import 'package:flutter/material.dart';

class FS3 extends StatelessWidget {
  const FS3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
